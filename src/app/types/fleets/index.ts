// Local types for the Fleet page. The data contracts (`VehicleSnapshot`,
// `VehicleQueueStats`, `FleetKpi`) live in `data/mockMetrics.ts`; the types
// here are UI-only filter unions and view-props.

export type FleetViewMode = "waiting" | "all";
