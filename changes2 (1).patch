diff --git a/scripts/dev.ps1 b/scripts/dev.ps1
index 74689e7..2556435 100644
--- a/scripts/dev.ps1
+++ b/scripts/dev.ps1
@@ -112,6 +112,12 @@ try {
     $Processes += $Frontend
 
     if ($DetectorEnabled -ne "false" -and $DetectorEnabled -ne "0") {
+        $DatFile = "shape_predictor_68_face_landmarks.dat"
+        if (-not (Test-Path $DatFile)) {
+            Write-Host "WARNING: ${DatFile} not found - detector disabled."
+            Write-Host "Download: Invoke-WebRequest -Uri http://dlib.net/files/${DatFile}.bz2 -OutFile ${DatFile}.bz2 (then decompress with 7-Zip or similar)"
+        }
+        else {
         Write-Host "Starting detector from ${DetectorScript} on camera ${DetectorCamera}"
         $DetectorArgs = @(
             $DetectorScript,
@@ -132,6 +138,7 @@ try {
             -NoNewWindow `
             -PassThru
         $Processes += $Detector
+        } # end else (.dat found)
     }
 
     Write-Host ""
